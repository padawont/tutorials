[tool.poetry]
name = "tutorials"
version = "0.1.0"
description = ""
authors = ["Ryan Harris Zubair"]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
